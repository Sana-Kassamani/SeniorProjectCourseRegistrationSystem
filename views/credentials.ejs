<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Credentials</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="main.css" rel="stylesheet">
  <style>
    body {
      animation: fadeIn 2s ease-in-out;
      background-color: #f0f0f0;
      font-family: Arial, sans-serif;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    .credentials-box {
      background-color: #98ccb2;
      border: 1px solid #310ca0;
      border-radius: 50%; /* Oval shape */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      padding: 20px;
      margin-top: 100px;
      max-width: 1300px;
      margin: auto;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      transition: background-color 0.3s, transform 0.3s, box-shadow 0.3s; /* Transition properties */
    }

    .credentials-box:hover {
      background-color: #729ec9; /* Change background color on hover */
      transform: scale(1.05); /* Increase size on hover */
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Change box shadow on hover */
    }

    .credentials-box h2 {
      color: rgb(12, 12, 12);
      margin-bottom: 10px;
      transition: font-size 0.3s ease; /* Smooth transition for font size */
    }

    .credentials-box hr {
      border: 0;
      border-top: 1px solid #729ec9;
      margin: 20px 0;
    }

    .credentials-box h2:hover {
      font-size: 1.2em; /* Larger font size on hover */
    }

    .graphics {
      display: flex;
      justify-content: center;
      margin-bottom: 20px;
    }

    /* Center the content horizontally and vertically */
    .credentials-box {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      text-align: center;
    }

    @keyframes rotate {
      0% {
        transform: rotate(0deg);
      }

      100% {
        transform: rotate(360deg);
      }
    }
  </style>
</head>


<body>
  <%# Your navigation and sidebar includes %>
  <%- include ('NavBar.ejs') -%>
  <div id="wrapper">
    <%- include ('SideBar.ejs') -%>
  <%- include ('NavBar.ejs') -%>
    <div id="wrapper">
      <%- include ('SideBar.ejs') -%>

    <div id="page-content-wrapper">
      
      <h2 class="text-white">Profile</h2>
      <br><br>
      <div class="credentials-box">
        
        <hr>
        <h2>Name: <%= data.FName %> <%= data.LName %></h2>
        <p> </p>
        <h2>ID Number: <%= studentIdentificationNumber %></h2>
        <p> </p>
        <h2>Email Address: <%= data.EmailAddress %></h2>
        <p> </p>
        <h2>Program Name: <%= data.ProgramName %></h2>
        <p> </p>
      </div>
    </div>
  </div>
        <div id="page-content-wrapper">
          <h2 class="text-white">Credentials</h2>

          <div class="container">
            <div class="id-card d-flex align-items-center">
              <img
                src="https://www.shutterstock.com/image-vector/default-avatar-profile-icon-social-600nw-1677509740.jpg"
                alt="Profile Picture" class="img-fluid">
              <div class="details">
                <h5 class="fw-bold">Name: <%= data.FName %>
                    <%= data.LName %>
                </h5>
                <p><strong>ID: </strong>
                  <%= studentIdentificationNumber %>
                </p>
                <p><strong>Program Name: </strong>
                  <%= data.ProgramName %>
                </p>
                <p><strong>Email Address:</strong>
                  <%= data.EmailAddress %>
                </p>
                <p style="text-align: right; color:grey;">valid till graduation</p>
              </div>
            </div>
          </div>

          <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
        </div>

  <script src="sidebar.js"></script>
    </div>
    <script src="sidebar.js"></script>

    <style>
      .id-card {
        background-color: #fff;
        /* White background */
        border: 1px solid #ddd;
        border-radius: 8px;
        padding: 15px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        max-width: 600px;
        margin: 20px 0;
        /* Remove center alignment */
        display: flex;
        flex-direction: column;
        /* Arrange content in column layout */
        position: relative;
        /* Required for absolute positioning of footer */
      }

      .id-card img {
        border-radius: 50%;
        max-width: 120px;
        height: auto;
      }

      .id-card .details {
        margin-left: 20px;
        flex: 1;
        /* Allow details to take available space */
      }

      .id-card .details h5 {
        margin-bottom: 10px;
      }

      .id-card .details p {
        margin: 0;
      }
    </style>
</body>

</html>


</html>